# 密码修改功能实施计划

1. 在认证模块增加当前密码校验和 Argon2 哈希更新函数。
2. 增加已登录的 `POST /api/auth/password` 接口，仅校验字段非空。
3. 为认证函数增加临时 SQLite 数据库测试，覆盖错误密码和成功更新。
4. 在前端 API 客户端增加修改密码请求。
5. 将“系统设置”空工作区替换为账户安全表单，处理确认密码、错误和成功状态。
6. 补充与现有工业终端视觉一致的响应式样式。
7. 运行后端测试、前端类型检查和生产构建。
8. 构建并部署服务器容器，验证当前会话保留、新密码有效、旧密码失效。
9. 提交并推送代码。
